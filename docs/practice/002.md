> 생성물. 직접 수정 금지. 답: ../commits/002.md

# 002 · feat(api): 운영 액션 endpoint — /internal/v1 위임

## 개요

admin-api의 본체. 정산 void/replay·환불·한도 변경·마켓 close/reopen을 **각 도메인 service로 위임**한다. admin-api는 검증·헤더 전파·결과 relay만 하고 비즈니스는 안 한다(thin proxy). 시작 가설("그냥 호출하면 됨")이 깨진 지점이기도 하다 — 다운스트림 실제 계약이 문서와 달랐다(아래).

## 작업 순서

1. 다운스트림 컨트롤러를 **직접 읽어** 실제 경로/바디 확인.
2. `Uuid7` — 액션 id 생성기(헤더+Idempotency-Key+audit PK 공용).
3. `AdminContext`(actor + actionId + traceId) + 인자 resolver + `WebConfig` 등록.
4. service별 `RestClient` 4개(`DownstreamClientConfig`) + `AdminHeaders` 전파.
5. `*Client` 4개(settlement/wallet/risk/market) + `DownstreamCalls`(에러 변환).
6. 컨트롤러 4개 + DTO + `AdminExceptionHandler`(RFC 7807, 상태 relay).

## 작업 내역

### 다운스트림 계약이 문서와 달랐다 (설계 결정 3개)

**책임:** admin이 가정한 경로/바디가 실제 다운스트림과 어긋날 수 있다. 각 service의 실제 `/internal/v1` 컨트롤러를 **직접 읽고**, 어긋나는 지점마다 admin 쪽 경로/바디/노출 여부를 결정하라. 좌열(admin 액션)은 노출된 계약이고, 우측 두 열(실제 계약·결정)이 재유도할 문제다.

```
| admin 액션        | 실제 다운스트림 계약 | 결정 |
|-------------------|--------------------|------|
| 마켓 close/reopen | // TODO: 실제 경로?  | // TODO: admin 경로에 무엇을 포함? |
| 환불(refund)      | // TODO: /refund 존재? | // TODO: 없으면 무엇으로 대체 + idempotency? |
| 계정 suspend      | // TODO: 대상 service 존재? | // TODO: endpoint 노출 여부? |
```

### HTTP 클라이언트는 RestClient

**책임:** Spring 6.1 동기 클라이언트 `RestClient`(webflux 불필요). service당 빈 1개, base URL + 타임아웃 고정. Boot가 미리 구성한 빌더를 **clone**해 Micrometer observation(→ traceparent 자동 전파)을 물려받는다. 4개 빈이 같은 타입이라 **생성자 파라미터 이름**(`settlementRestClient` 등)으로 주입 매칭(Boot가 `-parameters` 컴파일을 켜둔 덕분).

```java
@Configuration
public class DownstreamClientConfig {
  @Bean
  RestClient settlementRestClient(RestClient.Builder builder, DownstreamProperties properties) {
    // TODO: 아래 build(...)에 settlement base URL을 넘긴다. (wallet/risk/oddsFeed 빈도 동형)
  }

  private static RestClient build(
      RestClient.Builder builder, String baseUrl, DownstreamProperties properties) {
    // TODO: 자동구성 빌더를 clone → baseUrl 고정 → connect/read 타임아웃 requestFactory → build.
  }
}
```

### admin context 전파

**책임:** 모든 다운스트림 호출에 admin context 헤더(누가/어떤 액션/어떤 trace)를 찍어 각 service가 자기 audit에 남기게 한다. `AdminContext`는 컨트롤러 메서드 파라미터로 **선언만** 하면 `HandlerMethodArgumentResolver`(`WebConfig` 등록)가 SecurityContext의 Jwt + 새 actionId + MDC traceId로 만들어 주입한다.

```java
public static void apply(HttpHeaders headers, AdminContext context) {
  // TODO: actor id / actor role / action id(UUID v7) 헤더를 세팅하고,
  //       traceId가 있으면 trace 헤더도 세팅. (헤더 이름 상수는 AdminHeaders)
}
```

### UUID v7 직접 생성

**책임:** actionId는 영속 *전에* 필요(헤더로 먼저 보내야)해서 Hibernate `@UuidGenerator(style=TIME)`를 못 쓴다. RFC 9562 v7을 직접 생성 — 48-bit ms 타임스탬프를 prefix로 둬 **시간순 정렬**되게. 한 식별자가 헤더·Idempotency-Key·audit PK로 공용된다.

```java
public static UUID generate() {
  // TODO: 48-bit ms 타임스탬프를 상위 비트에 두고 버전 nibble(7)+randA를 OR해 msb를,
  //       variant '10' 비트 + 62-bit 난수로 lsb를 구성해 new UUID(msb, lsb) 반환.
}
```

### 에러 처리 = thin proxy의 투명성

**책임:** 다운스트림 non-2xx는 **그대로 relay**한다(모든 service가 이미 problem+json이라 운영자가 진짜 이유를 본다). 도달 불가/타임아웃만 502/504로 변환. `@PreAuthorize` 거부(`AccessDeniedException`)는 advice가 403으로(필터단 핸들러와 동일 모양).

```java
static <T> T execute(String service, Supplier<T> call) {
  // TODO: call 실행. non-2xx 응답 예외(RestClientResponseException)는 상태+바디 보존해 relay 예외로,
  //       그 외 전송 실패(RestClientException)는 502/504 예외로 변환. (응답계열을 먼저 catch)
}
```

## 검증

- `mvn -Dtest=DelegationTest test` → 10 케이스 green(WireMock 1개로 4 service 스텁, Docker 불필요): 위임 경로/바디/헤더, role 매트릭스, 상태 relay(404), 502(fault)/504(timeout).
- `mvn -Dtest=Uuid7Test test` → 2 케이스: 생성 id의 버전 nibble == 7 + variant + 연속 생성이 시간순으로 단조 증가.
