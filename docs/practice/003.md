> 생성물. 직접 수정 금지. 답: ../commits/003.md

# 003 · feat(audit): 모든 admin 액션을 Kafka + audit_log에 이중 박제

## 개요

ADR-0011의 핵심 책임. 운영 액션은 돈·계정을 건드리므로 "누가 언제 무엇을 했나"가 사고 시 1차 증거다. 두 독립 저장소에 같은 기록을 남겨(action id로 cross-verify) 한 쪽이 의심스러우면 다른 쪽과 대조한다:

- **내구(durable)**: `audit_log` 테이블(admin-api가 소유하는 유일한 테이블).
- **스트리밍**: Kafka `admin.action` 토픽(`AdminActionRecorded`, Avro).

## 작업 순서

1. Avro 스키마 `AdminActionRecorded.avsc`(avro-maven-plugin이 .java 생성).
2. `AuditLogEntity`(JPA) + `AuditLogRepository` + Flyway `V1__audit_log.sql`.
3. `AvroSerializer` + `AuditKafkaConfig`(byte[] producer) + `AdminActionPublisher`.
4. `AuditService` — DB 먼저, 그다음 Kafka(best-effort).
5. `@Audited` 애너테이션 + `AuditAspect`(AOP) — 모든 액션에 자동 적용.
6. 6개 컨트롤러 메서드에 `@Audited` + `GET /admin/v1/audit-logs`(offset page).

## 작업 내역

### annotation 기반 AOP 감사

**책임:** 각 컨트롤러 메서드에 `@Audited(action, target SpEL, reason SpEL)`만 달면 aspect가 결과를 보고 기록한다. aspect를 `@PreAuthorize`보다 **바깥**에서 돌게 만들어 권한 거부(403)도 감사한다 — "권한 없는 자가 시도했다"는 보안상 중요한 기록. 성공/실패 + HTTP 상태는 반환 `ResponseEntity` 또는 던져진 예외에서 도출한다.

```java
// 사용(노출된 계약): 컨트롤러 메서드에 애너테이션만 단다.
@PostMapping("/{betId}/void")
@PreAuthorize("hasAnyRole('ADMIN','TRADER')")
@Audited(value = AdminAction.SETTLEMENT_VOID, target = "#betId")
public ResponseEntity<Void> voidBet(@PathVariable UUID betId, AdminContext context) { /* 위임 — 002 */ }

@Aspect
@Component
@Order(/* TODO: @PreAuthorize 메서드보안보다 '바깥'에서 돌 우선순위 — 권한 거부까지 감사하려면? */)
public class AuditAspect { /* @Around 본문은 아래 단위 */ }
```

### ★ AOP 인자 바인딩 함정 (시간 까먹은 곳)

**책임:** 교과서대로 `@Around("@annotation(audited)")` + 파라미터 `Audited audited` 바인딩을 하면 전 endpoint가 500(`Required to bind 2 arguments, but only bound 1 (JoinPointMatch was NOT bound)`). `argNames` 명시도 안 통한다. 결론은 **바인딩을 버리고 advice 안에서 리플렉션으로 애너테이션을 읽는 것** — 더 단순·견고하다(C/C++엔 "포인트컷 변수가 파라미터에 바인딩된다"는 레이어 자체가 없다).

```java
@Around("@annotation(com.sportsbook.admin.audit.Audited)")
public Object audit(ProceedingJoinPoint joinPoint) throws Throwable {
  // TODO: 파라미터 바인딩 대신, joinPoint 시그니처의 Method에서 @Audited를 '리플렉션으로' 읽는다.
  //       인자에서 AdminContext를 찾고, target/reason SpEL을 평가(아래 단위).
  //       proceed() 성공 → 결과의 HTTP 상태로 기록, 예외 → 예외의 상태로 기록한 뒤 rethrow.
}
```

### SpEL로 target/reason 추출

**책임:** `target = "#betId"`, `reason = "#request.reason()"` 같은 SpEL을 **메서드 인자에 대해** 평가해 감사 레코드의 target/reason을 뽑는다.

```java
private String evaluate(String expression, ProceedingJoinPoint joinPoint) {
  // TODO: 식이 비었으면 null. 메서드 + 인자 + 파라미터 이름으로 EvaluationContext를 만들고
  //       식을 파싱·평가해 문자열로 반환(없으면 null).
}
```

### 이중 박제 + best-effort 트레이드오프

**책임:** DB를 먼저(가장 내구) 쓰고 Kafka를 발행한다. **둘 다 실패해도 응답은 깨뜨리지 않는다** — 다운스트림 액션은 이미 일어났으니, 감사 실패로 운영자에게 "실패"라 잘못 보고하면 안 된다. 실패는 로그 + 메트릭(`admin.audit.write.failure{store=db|kafka}`). admin-api는 다운스트림과 한 트랜잭션이 아니라 이론상 "액션됨/감사 빠짐" 갭이 있다(무손실은 outbox 필요 — V1 밖).

```java
public void record(
    AdminContext context, AdminAction action, String target, String reason,
    String outcome, int httpStatus) {
  // TODO: audit_log 행 저장을 try로 감싸 실패 시 메트릭(store=db)+에러로그만 남기고 rethrow 금지.
  //       이어서 Avro 이벤트 발행을 try로 감싸 실패 시 메트릭(store=kafka)+에러로그만.
  //       (DB를 먼저 = Kafka가 죽어도 내구 기록은 보존)
}
```

### Avro byte[] 발행

**책임:** `admin.action`은 `(key=actorId, value=byte[] Avro)`. Schema Registry 없이 `.avsc`만(ADR-0014, avro-maven-plugin이 `.java` 생성). V1엔 consumer가 아직 없다 — DB가 1차 조회원, Kafka는 사본.

```java
// TODO: AvroSerializer — AdminActionRecorded를 byte[]로 직렬화.
//       AuditKafkaConfig — <String, byte[]> producer factory / template 구성(bootstrap은 application.yml).
//       AdminActionPublisher — key=actorId로 admin.action 토픽에 발행.
```

### 조회 endpoint

**책임:** `GET /admin/v1/audit-logs?from=&to=&actor=` — offset pagination(ADR-0004, admin은 cursor 아님). **파라미터 바인딩** 쿼리로 SQL injection을 차단한다(문자열 결합 금지 — 004에서 공격으로 검증). 읽기라 자기 자신은 감사하지 않는다.

```java
// TODO: AuditLogRepository에 파라미터 바인딩 쿼리(actor가 null이면 전체, 아니면 일치 — 문자열 결합 금지).
//       AuditLogController가 from/to/actor + page/size를 받아 OffsetPage<AuditLogView>로 반환. @Audited 없음(읽기).
```

## 검증

- `mvn -Dtest=AuditIntegrityTest test` → 3 케이스: 성공 액션이 DB·토픽에 **action id로 일치**(cross-verify), 다운스트림 404는 FAILED/404로 기록, 조회 endpoint가 newest-first 반환.
  ⚠ 실제 PostgreSQL(Testcontainers) + embedded Kafka + WireMock이 필요하다 → **Docker 데몬이 떠 있어야** 실행된다. Docker 없는 환경에선 컨테이너 기동 단계에서 실패(설계상 인프라 의존 — 코드 결함 아님).
- 메트릭 `admin.audit.write.failure{store=db|kafka}`가 저장 실패 시 증가하는지.
