# 006 · docs(readme): 보안 / 성능 증명 섹션

## 개요

dev 단계 마지막 커밋. README의 "성능/보안" 자리(scaffold 때 placeholder)를 **실제 증명 매트릭스**로 교체한다. 해외 면접관이 README 한 번에 "이 서비스가 무엇을 증명했나"를 보게 하는 게 목적(ADR-0016).

> 참고: README 후속 변경은 dev 커밋과 섞지 않고 단독 `docs(readme):` 커밋(SESSION_STARTUP 규칙). 그래서 Task 5의 보안/부하 코드와 별도 커밋.

## 작업 순서

1. "성능/보안" placeholder를 보안 매트릭스 + 감사 무결성 + 부하 framing으로 교체.

## 작업 내역

세 블록으로 정리:

- **보안 매트릭스** — 공격 시도 ↔ 기대 응답(401/403/무력화) ↔ 그걸 증명하는 테스트(`AdminSecurityTest`/`SecurityProbeTest`/`DelegationTest`)를 표로. 면접에서 "보안 어떻게 보장?"에 표 하나로 답.
- **감사 무결성** — DB·Kafka 이중 박제 + action id cross-verify + 실패도 FAILED 기록. `AuditIntegrityTest`가 보장.
- **부하** — 낮은 우선순위임을 명시하고 `load-test/`로 포인터.

전부 `mvn verify`로 재현된다고 못박음(Testcontainers → Docker 필요).

## 결과

README 1파일 변경(+26/-3). 영문 박스(상단)는 이미 보안 focus를 담고 있어, 한국어 본문의 성능/보안 절만 충실화. (최종 영문 박스 확장은 retrospective의 `docs(readme): finalize`에서.)

## 요약

- placeholder → 보안 매트릭스 + 감사 무결성 + 부하 framing.
- 모든 주장에 "그걸 증명하는 테스트" 명시.

## 다음 작업

dev 완료. 사용자 push 후 retrospective(reflection→commits→readme finalize).

## 핵심 확인

- README "성능 / 보안" 절 ↔ 실제 테스트 클래스 대응.

## 기억/설명 Level

- **L1**: README 어디에 무엇이 있는지.
- **L2**: 왜 README 변경이 단독 커밋인지(dev 노이즈 분리).
- **L3**: (없음)
